Проект создания коротких ссылок

Сервис сокращения ссылок с поддержкой ограничения количества переходов, времени жизни (TTL), уникальности ссылок для каждого пользователя, а также с возможностью редактирования и удаления собственных ссылок.
Управление сервисом осуществляется через консольный интерфейс (CLI).

Возможности сервиса:
- Сокращение длинных URL в короткие уникальные ссылки
- Уникальные ссылки даже при одинаковом исходном URL у разных пользователей
- Ограничение на количество переходов
- Время жизни ссылки (TTL) — автоматическое удаление “протухших” ссылок
- UUID-идентификация пользователя без авторизации
- Редирект через открытие браузера
- Управление ссылками: просмотр, редактирование, удаление
- Уведомления об истечении лимита или TTL
- Поддержка нескольких пользователей и нескольких ссылок на пользователя

👉 Установка и запуск
 1. Склонируйте проект:
      git clone https://github.com/Timon27M/Shortened-links-service.git
 2. Убедитесь, что установлен JDK 21.
 3. Соберите приложение:
      mvn clean package
 4. Запустите:
    java -jar target/Shortened-links-service-1.0-SNAPSHOT-shaded.jar 

👉 Как пользоваться сервисом
1. После запуска приложения в консоли появляется главное меню:

=== Генератор коротких ссылок ===
1. Создать короткую ссылку.
2. Перейти по короткой ссылке.
3. Админ права.
4. Выход.

Эти пункты являются основой управления сервисом.

2. Создать короткую ссылку

Позволяет создать новую сокращённую ссылку.

При выборе система запросит:

- логин
- исходный URL
- лимит переходов
- время жизни ссылки (в минутах)

После чего система выведет и сохранит короткую ссылку.
Также если это первоя создание ссылки для данного пользователя система выведет уникальный UUID пользователя.

3. Переход по ссылке

    Для того чтобы перейти по ссылке нужно ввести короткую ссылку. Система автоматически откроет исходную сссылку в браузере.

4. Админ права

В данном разделе у пользователя появляется возможноссть редактировать основные параметры ссылки.

После перехода в данный раздел нужно ввести:

- Логин
- UUID пользователя

Далее у пользователя в консоли появится меню

=== Действия администратора: ===
1. Обновить лимит переходов по ссылке.
2. Обновить время жизни короткой ссылки.
3. Удалить ссылку.
4. Удалить акканунт.
5. Показать параметры ссылки.
6. Назад.

Где можно получать, изменять основные параметры и удалять ссылки. Также реализовано удаление пользователя.

Все данные сохраняются в отдельном файле UsersDB.json.

👉 Уведомления
Программа уведомляет пользователя:

- если лимит переходов исчерпан
- если ссылка просрочена по TTL
- если нельзя редактировать/удалять чужую ссылку
- если URL некорректен

👉 Тестирование
В проекте реализовано тестирование основных сервисов:

- AdminService
- UrlLimitService
- UrlService
- UrlValidationService
- UserService

Протестированы все основные методы в разных состояниях.

👉 CI/CD:
Реализовано CI/CD в github. Триггер запускает workflow при каждом пуше в репозиторий.

Настроены основные jobs:
1. Check-linter - происходит проверка код стайла.
- шаг Checkout code: Скачивает репозиторий в виртуальную машину CI.
- шаг Set up JDK 21: Устанавливает Java 21 и настраивает Maven cache.
- шаг Validate code format: Проверяет форматирование кода (Java, XML, etc.) через Spotless.

2. test - запускаются тесты.
- шаг Checkout code: Скачивает репозиторий в виртуальную машину CI.
- шаг Set up JDK 21: Устанавливает Java 21 и настраивает Maven cache.
- шаг Run tests: Запускает все тесты из src/test/java.

3. build - сборка проекта. 
- шаг Checkout code: Скачивает репозиторий в виртуальную машину CI.
- шаг Set up JDK 21: Устанавливает Java 21 и настраивает Maven cache.
- шаг Build project: делает компиляцию.
- шаг Run linter (Checkstyle): линтинг правил Java-кода.
- шаг Verify Java version compatibility: Проверяет совместимость кода с Java 21.

4. docker-build - сборка в docker контейнер.
- шаг Checkout code: Скачивает репозиторий в виртуальную машину CI.
- шаг Log in to GitHub Container Registry: делает автоматический логин. Чтобы отправить Docker-образ в GHCR (GitHub Container Registry)
- шаг Prepare image name: Формирует имя Docker-образа и сохраняет имя образа как output.
- шаг Build Docker image: Запускает Docker build.
- шаг Push Docker image: Загружает образ в GHCR.

5. java-ci-check - информирует об успешном завершении проверок.
- шаг All checks completed: Сообщает об успешных проверках.

Также в проекте добавлены проверки при коммите!

👉 Деплой проекта реализован на render.com
- Как только код попадает в main ветку, запускается автоматический деплой.

- деплой собирается и запускается, но сразу падает так как проект консольный!
